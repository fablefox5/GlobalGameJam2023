using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chancla : MonoBehaviour
{
    // Start is called before the first frame update
    void ChanclaDestroy()
    {
        Destroy(this.gameObject);
    }
}
